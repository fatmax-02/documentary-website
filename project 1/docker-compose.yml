services:
  server:
    build: ./server
    ports:
      - "4010:4007"
    environment:
      - PORT=4007
      - MONGO_URI=mongodb+srv://admin:project123@cproject.omqqfyp.mongodb.net/Login?retryWrites=true&w=majority

  client:
    build: ./client
    ports:
      - "3010:3000"
    depends_on:
      - server
    environment:
      - REACT_APP_API_URL=http://server:4007